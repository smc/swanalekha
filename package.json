{
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "js:build": "cd js && npm ci && npm run build",
    "build": "npm run js:build && npm run docs:build",
    "dev": "npm run js:build && npm run docs:dev"
  },
  "devDependencies": {
    "vuepress": "^1.8.2"
  }
}
